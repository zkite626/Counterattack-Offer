services:
  postgres:
    image: postgres:16-alpine
    container_name: counterattack-offer-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: counterattack_offer
    ports:
      - "5432:5432"
    volumes:
      - counterattack_offer_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d counterattack_offer"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  counterattack_offer_postgres_data:
