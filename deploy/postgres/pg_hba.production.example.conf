# 仅示例关键规则：生产请合并到服务器现有 pg_hba.conf。
# 禁止公网裸露，应用只从本机或内网访问 PostgreSQL。
local   all                  postgres                   peer
host    counterattack_offer  counterattack_offer_app    127.0.0.1/32    scram-sha-256
host    counterattack_offer  counterattack_offer_app    ::1/128         scram-sha-256
